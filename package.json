{
  "name": "amigo-fineliner",
  "version": "0.1.0",
  "private": true,
  "description": "Root build entry for Cloudflare Workers (delegates to scripts/cf-build.sh).",
  "scripts": {
    "build": "bash scripts/cf-build.sh"
  }
}
