# Generated by ts-rs and committed (ADR-014). CI fails on a dirty diff here, so
# a formatter must never touch it — reformatting would break the `git diff
# --exit-code` check in the rust CI job on the very next run.
src/lib/core/generated/

# Generated by wasm-pack, gitignored.
src/lib/wasm/pkg/

# Build output.
dist/

# pnpm owns this file's formatting.
pnpm-lock.yaml
